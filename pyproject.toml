[project]
name = "indagine"
version = "0.1.0"
description = "Meta-agent system that debugs other agents when they fail"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
