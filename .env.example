# Azure AI Foundry / Projects
# You can set either the FOUNDRY_* vars (preferred in this repo) or the AZURE_AI_* vars.

# Foundry project endpoint URL, e.g.
# https://<account>.services.ai.azure.com/api/projects/<project>
FOUNDRY_PROJECT_ENDPOINT=

# Model deployment name as shown in Foundry "Models + endpoints"
FOUNDRY_MODEL_DEPLOYMENT=

# Alternative env var names used by the Azure SDK docs
AZURE_AI_PROJECT_ENDPOINT=
AZURE_AI_MODEL_DEPLOYMENT_NAME=

# Optional: send OpenTelemetry traces to Azure Monitor / App Insights
APPLICATIONINSIGHTS_CONNECTION_STRING=

# Optional auth path (recommended for CI / non-interactive)
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
